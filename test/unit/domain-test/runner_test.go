package domain_test

/*
func TestJudge0_CodeRun(t *testing.T) {
	j := &runner.Judge0{}
	submission := runner.RunnerSubmission{
		SourceCode:   "print('Hello, World!')",
		LanguageId:   1,
		CPUTimeLimit: 1.0,
		MemoryLimit:  128,
		Testcase: []runner.RunnerTestcaseSubmission{
			{Input: "1 2", ExpectedOutput: "3"},
		},
	}
	result := j.CodeRun(submission)
	if len(result.TestResult) == 0 {
		t.Fatalf("CodeRun 应返回至少一个测试结果")
	}
}

func TestJudge0_GetLanguage(t *testing.T) {
	j := &runner.Judge0{}
	langs, err := j.GetLanguage()
	if err != nil {
		t.Fatalf("GetLanguage 调用失败: %v", err)
	}
	if len(langs) == 0 {
		t.Fatalf("GetLanguage 应返回至少一个语言")
	}
}
*/
